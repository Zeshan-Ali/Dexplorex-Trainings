#include<stdio.h>
void main()
{
    int age = 29;
    float height = 3.7;
    char grade = 'A';
    printf("%d the age is: ",age);
    printf("\nthe height is: %f",height);
    printf("\nthe grade is:%c",grade);

    printf("the age is %d, the hehight is %f, the grade is %c",age,height,grade);
}
